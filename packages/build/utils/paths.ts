// 路径信息
import { resolve } from "path";

// 打包目录
export const distPath = resolve(__dirname, "../");

//组件库根目录
export const componentPath = resolve(__dirname, "../../components");

//组件样式目录
export const stylePath = resolve(__dirname, "../../theme-chalk");
